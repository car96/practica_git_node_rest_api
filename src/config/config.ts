const config = {
    app: {
        host: 'localhost',
        port: 4000,
        version: 'v1.0'
    },
    db: {
        host: 'localhost',
        port: 27017,
        name: 'pokemon_db'
    }
};
   
module.exports = config;